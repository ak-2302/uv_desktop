# uv Desktop 要件定義書

## 1. 文書情報

| 項目 | 内容 |
| --- | --- |
| 文書名 | uv Desktop 要件定義書 |
| 対象バージョン | v1 |
| 作成日 | 2026-09-25 |
| 対象リポジトリ | `uv_desktop` |
| 文書の目的 | uvをGUIから操作するクロスプラットフォームデスクトップアプリの開発要件を定義する |

## 2. プロダクト概要

uv Desktopは、PythonパッケージマネージャーuvをGUIから操作するネイティブデスクトップアプリである。Python初心者から中級者が、コマンドラインを直接操作しなくてもPythonプロジェクトの作成、依存関係管理、仮想環境の同期、コマンド実行を行えることを目的とする。

アプリはElectronをデスクトップシェルとして使用し、Renderer ProcessではReact、TypeScript、Viteを使用する。uvはアプリに同梱せず、ユーザー環境にインストールされている実行ファイルを検出して使用する。

## 3. 背景と解決する課題

### 3.1 背景

uvは高速なPythonパッケージマネージャーであり、プロジェクト作成、依存関係管理、ロック、仮想環境同期、コマンド実行を提供する。一方、uvの操作は基本的にCLIを前提とするため、Python開発に慣れていない利用者には、コマンドやオプション、プロジェクトファイルの関係が分かりにくい。

### 3.2 解決する課題

- uvのコマンドを覚えなくても主要なプロジェクト操作を実行できない
- `pyproject.toml`、`uv.lock`、`.python-version`、`.venv`の役割が分かりにくい
- 依存追加や同期で何が変更されるのか、実行前に確認しにくい
- エラーがCLIログ中心で、初心者が原因と対応方法を判断しにくい
- OSごとのuv実行ファイルや環境変数の差異を利用者が意識する必要がある

## 4. 目的と成功条件

### 4.1 目的

1. GUIだけでuvプロジェクトを新規作成できるようにする。
2. 既存プロジェクトの依存関係と仮想環境の状態を視覚的に確認できるようにする。
3. 状態を変更する操作の内容を実行前に確認できるようにする。
4. uvの実行ログを保持し、失敗理由と推奨対応を確認できるようにする。
5. macOS、Windows、Linuxで同じ主要操作を提供する。

### 4.2 v1の成功条件

- 初心者が画面の案内だけでプロジェクトを作成できる。
- GUIから追加・削除した依存関係が`pyproject.toml`と`uv.lock`に反映される。
- `uv sync`後の仮想環境状態を画面で確認できる。
- uv未導入、ネットワークエラー、依存解決失敗を利用者が理解できる。
- 操作中にRenderer Processが固まらず、長時間処理の進捗とログが表示される。

## 5. 対象ユーザーと利用シナリオ

### 5.1 対象ユーザー

- Pythonを学習中の初心者
- CLI操作には慣れていないが、Python開発を行う中級者
- 依存関係や仮想環境の状態をGUIで確認したい開発者

### 5.2 主な利用シナリオ

#### シナリオA：新規プロジェクトを作成する

1. アプリを起動する。
2. 「新規プロジェクト」を選択する。
3. プロジェクト名、保存先、アプリケーション／ライブラリ種別、Pythonバージョンを指定する。
4. 実行される`uv init`相当の内容を確認する。
5. プロジェクトを作成し、概要画面を表示する。

#### シナリオB：依存パッケージを追加する

1. 既存プロジェクトを開く。
2. 依存関係画面でパッケージ名とバージョン指定を入力する。
3. 通常依存または開発用依存を選択する。
4. 実行コマンドと変更対象ファイルを確認する。
5. `uv add`相当の処理を実行し、結果を画面に反映する。

#### シナリオC：環境を同期する

1. プロジェクト概要画面で環境状態を確認する。
2. 「同期」を実行する。
3. `uv sync`の進行状況とログを確認する。
4. 成功時にロックファイルと仮想環境の状態を更新する。

#### シナリオD：プロジェクト内のコマンドを実行する

1. コマンド実行画面を開く。
2. `uv run`で実行するコマンドを入力する。
3. 作業ディレクトリと実行内容を確認する。
4. 実行し、標準出力、標準エラー、終了コードを確認する。

## 6. 対応環境と前提条件

### 6.1 対応OS

- macOS
- Windows
- Linux

OS固有の実装差異は、パス区切り、実行ファイル探索、環境変数、シェル、プロセス終了、権限、ファイルダイアログを考慮する。

### 6.2 前提条件

- uvはユーザーが別途インストールする。
- アプリは起動時またはプロジェクト操作前にuvの存在を検出する。
- uv未導入時は、公式ドキュメントへのリンクとOS別の導入方法を表示する。
- 対象プロジェクトはローカルファイルシステム上にある。
- v1ではアカウント、クラウド同期、プロジェクトデータの外部送信を行わない。

## 7. 技術方針

### 7.1 採用技術

| 層 | 技術 | 採用理由 |
| --- | --- | --- |
| デスクトップシェル | Electron | macOS、Windows、Linuxに対応し、Node.jsによるプロセス・ファイル・OS連携を利用できる |
| Renderer UI | React | 画面と再利用可能なコンポーネントを分離しやすい |
| 言語 | TypeScript | IPCや状態モデルの型を共有し、実行パラメータの誤りを減らす |
| 開発サーバー・ビルド | Vite | React開発時の高速なフィードバックと分かりやすい構成を得られる |
| バックエンド操作 | Electron Main Process | uvの検出、子プロセス実行、ファイルダイアログ、OS連携をRendererから分離する |

### 7.2 Electronアーキテクチャ

- Main Processはアプリのライフサイクル、ウィンドウ、uv子プロセス、ファイルダイアログを管理する。
- Renderer Processは画面表示、入力、状態表示のみを担当する。
- RendererからNode.js APIや`child_process`を直接利用できない構成にする。
- Preloadスクリプトで許可したAPIだけを`contextBridge`経由で公開する。
- `contextIsolation`を有効にする。
- 可能な範囲でRendererの`sandbox`を有効にする。
- IPCはコマンド名、引数、作業ディレクトリを型付きの許可リストで検証する。
- 任意のシェル文字列をそのままシェル経由で実行しない。引数配列を利用し、必要な場合のみ明示的なシェル実行を許可する。

## 8. uv実行方式

### 8.1 対象コマンド

v1では次のコマンドを対象とする。

- `uv init`
- `uv add`
- `uv remove`
- `uv sync`
- `uv lock`
- `uv tree`
- `uv run`

### 8.2 uvの検出

- OSの実行ファイル探索機能を利用してuvを検出する。
- 設定画面からuv実行ファイルのパスを手動指定できるようにする。
- 起動時または設定画面で`uv --version`を実行し、パスとバージョンを表示する。
- uvが見つからない場合は、処理を開始せず導入案内を表示する。
- v1ではuvを自動ダウンロード・自動インストール・アプリ内同梱しない。

### 8.3 プロジェクトファイル

| ファイル／ディレクトリ | 用途 |
| --- | --- |
| `pyproject.toml` | プロジェクト名、Python要件、依存関係、ビルド設定を定義する |
| `uv.lock` | 解決済み依存関係を固定する |
| `.python-version` | プロジェクトで使用するPythonバージョンを指定する |
| `.venv` | プロジェクト用の仮想環境を保持する |

## 9. 機能要件

### FR-001：uv状態の確認

- アプリ起動後にuvの検出状態を表示する。
- uvのパス、バージョン、検出日時を表示する。
- uv未導入、実行権限不足、実行失敗を区別して表示する。

### FR-002：新規プロジェクト作成

- プロジェクト名と保存先を入力できる。
- アプリケーションまたはライブラリを選択できる。
- Pythonバージョンを指定できる。
- 既存の`pyproject.toml`が存在する場合は上書きせず、競合として通知する。
- 実行前に対象パスと作成内容を確認できる。
- 作成完了後はプロジェクト概要画面を表示する。

### FR-003：既存プロジェクトの読み込み

- フォルダ選択ダイアログでプロジェクトを選択できる。
- 選択フォルダまたは親フォルダに`pyproject.toml`があるか検出する。
- `pyproject.toml`がない場合は、uvプロジェクトとして読み込めない理由を表示する。
- `uv.lock`、`.python-version`、`.venv`の有無と状態を表示する。

### FR-004：プロジェクト概要表示

- プロジェクト名、パス、Python要件、uvバージョンを表示する。
- ロックファイルの有無と更新状態を表示する。
- 仮想環境の有無と同期状態を表示する。
- 最終操作、成功／失敗、終了コードを表示する。
- 「依存関係管理」「同期」「コマンド実行」「ログ確認」へ遷移できる。

### FR-005：依存関係の追加

- パッケージ名を入力できる。
- バージョン指定、extras、環境マーカーを入力できる設計とする。
- 通常依存、任意依存、開発用依存を区別できる。
- 実行前に`uv add`相当のコマンドと変更対象を表示する。
- 成功後に`pyproject.toml`、`uv.lock`、環境状態を再読み込みする。

### FR-006：依存関係の削除

- 現在の依存関係一覧から削除対象を選択できる。
- 削除前にパッケージ名、依存区分、対象ファイルを確認する。
- `uv remove`相当の処理を実行する。
- 削除に失敗した場合はファイルを再読み込みし、表示を実状態に戻す。

### FR-007：依存関係と環境の同期

- `uv sync`相当の処理を実行できる。
- 実行中は進行中状態、ログ、キャンセル操作を表示する。
- 成功時に仮想環境、ロックファイル、依存一覧を更新する。
- 失敗時は終了コード、標準エラー、推奨対応を表示する。

### FR-008：依存ツリー表示

- `uv tree`相当の結果をツリーまたは読みやすいテキストで表示する。
- 直接依存と間接依存を区別する。
- 実行日時と対象プロジェクトを表示する。

### FR-009：コマンド実行

- プロジェクトの作業ディレクトリを表示する。
- `uv run`で実行するコマンドを入力できる。
- 実行前に作業ディレクトリ、コマンド、引数を確認する。
- 標準出力、標準エラー、終了コード、実行時間を表示する。
- 実行中のプロセスを停止できる。

### FR-010：ログとエラー表示

- 操作ごとに実行コマンド、開始時刻、終了時刻、終了コードを記録する。
- 標準出力と標準エラーを別表示できる。
- ログをテキストとしてコピーできる。
- エラー内容に応じて、uv未導入、ネットワーク、権限、依存解決などの分類を表示する。
- ログにアクセストークンなどの秘密情報を含めないため、既知の認証情報パターンをマスクする。

### FR-011：設定

- uv実行ファイルのパスを確認・変更できる。
- テーマをライト、ダーク、OS設定に合わせるから選択できる。
- ログ保持の有無と保持期間を設定できる。
- 言語設定は将来の日本語・英語切り替えを想定した構造にする。
- 外部送信やアカウント連携を行わないことを明示する。

## 10. 画面要件

| 画面 | 主な内容 | 主な操作 |
| --- | --- | --- |
| 初回セットアップ／uv状態 | uvの検出、バージョン、導入案内 | 再検出、パス指定、公式サイトを開く |
| プロジェクト選択 | 最近のプロジェクト、フォルダ選択 | 新規作成、既存フォルダを開く |
| 新規プロジェクト | 名前、保存先、種別、Pythonバージョン | 作成、キャンセル |
| プロジェクト概要 | ファイル、依存、Python、仮想環境の状態 | 同期、依存管理、実行、ログ表示 |
| 依存関係管理 | 通常依存、開発依存、任意依存、バージョン | 追加、削除、ツリー表示 |
| Python・仮想環境 | Python要件、`.python-version`、`.venv` | 状態確認、同期 |
| コマンド実行 | コマンド、作業ディレクトリ、実行結果 | 実行、停止、ログコピー |
| ログ・エラー | 実行履歴、標準出力、標準エラー | 検索、コピー、再実行 |
| 設定 | uvパス、テーマ、言語、ログ設定 | 保存、初期化 |

### 10.1 UI設計方針

- 最初の画面で「新規作成」と「既存プロジェクトを開く」を明確にする。
- 状態変更操作は、対象、実行コマンド、変更ファイルを確認できるようにする。
- 成功、失敗、処理中、空状態、未導入状態を明確に区別する。
- アイコンだけのボタンには可視ラベルまたはアクセシブルなラベルを付ける。
- すべての主要操作をキーボードで実行できるようにする。
- フォーカス表示、コントラスト、見出し階層、フォームラベルを設計する。
- 長いパッケージ名、長いパス、大量ログ、狭いウィンドウ幅でも内容が欠落しないようにする。

## 11. 非機能要件

### NFR-001：性能

- 初期画面はuv検出処理の結果を待ち続けず、検出中状態を表示する。
- uv実行はRendererのメインスレッドをブロックしない。
- 大量ログは仮想スクロールまたは段階的描画を利用し、UI操作を阻害しない。
- 子プロセス終了時に確実にIPC購読を解除し、メモリリークを防ぐ。

### NFR-002：信頼性

- uvコマンドの終了コードを必ず取得する。
- 子プロセスの異常終了、タイムアウト、ユーザーによる停止を区別する。
- 実行後は必要に応じてファイルを再読み込みし、表示と実体の不一致を避ける。
- 操作失敗時に、可能な範囲で変更前の状態と変更後の状態を表示する。

### NFR-003：セキュリティ

- RendererへNode.js API、ファイルシステムAPI、子プロセスAPIを直接公開しない。
- IPCリクエストのコマンド名、引数、パスをMain Processで検証する。
- 選択されたプロジェクトフォルダ以外を、明示的な操作なしに変更しない。
- シェルインジェクションを防止するため、引数を配列として扱う。
- ログや設定に秘密情報を平文で保存しない。
- 外部ネットワーク通信はuvが依存解決で必要とする通信に限定し、アプリ独自のテレメトリはv1では実装しない。

### NFR-004：配布

- v1は開発版および手動配布を対象とする。
- macOS notarization、Windowsコード署名、Linuxパッケージの正式配布は将来要件とする。
- 自動更新は実装しない。

## 12. エラー要件

| エラー | 検出方法 | 表示・対応 |
| --- | --- | --- |
| uv未導入 | 実行ファイル検出失敗 | OS別の公式導入案内、再検出、手動パス指定 |
| uvバージョン不一致 | `uv --version`の結果 | 必要バージョン、検出バージョン、対応方法を表示 |
| `pyproject.toml`不在 | プロジェクト読み込み時のファイル確認 | uvプロジェクトとして開けない理由を表示 |
| 不正な依存指定 | uvの終了コードと標準エラー | 入力内容、対象パッケージ、修正候補を表示 |
| PyPI・ネットワーク失敗 | uvの終了コードと標準エラー | 接続状態、再試行、オフライン時の制約を表示 |
| Python未導入・不一致 | uvの実行結果 | 必要バージョンと導入・変更方法を表示 |
| 権限不足 | 子プロセス・ファイル操作の失敗 | 対象パスと必要な権限を表示。危険な権限昇格は自動実行しない |
| 既存ファイルとの競合 | 作成前のファイル確認 | 上書きせず、別フォルダ選択またはキャンセルを案内 |
| プロセス中断 | ユーザー操作または異常終了 | 中断理由、部分変更の可能性、再実行方法を表示 |

## 13. v1の対象外

- uv実行ファイルのアプリ内同梱
- uvの自動インストール、自動アップデート
- Electronアプリの自動更新
- macOS、Windows、Linuxの署名付き正式配布とストア配布
- `uv tool`の高度な管理
- `uv publish`、`uv build`のGUI管理
- uv workspaceの高度な管理
- クラウド同期、アカウント、チーム共有
- 外部パッケージレジストリの管理画面
- IDE機能、コードエディタ、デバッガ

## 14. 受け入れ条件

### AC-001：新規作成

- uvが検出された環境で、GUIからアプリケーションまたはライブラリのプロジェクトを作成できる。
- 作成後に`pyproject.toml`が生成され、プロジェクト概要画面が表示される。
- 既存の`pyproject.toml`がある場所では、上書きせずに処理を中止する。

### AC-002：既存プロジェクト

- `pyproject.toml`を含むフォルダを開くと、プロジェクト情報が表示される。
- `uv.lock`、`.python-version`、`.venv`の有無と状態が表示される。
- `pyproject.toml`がない場合は、理由を表示して安全に戻れる。

### AC-003：依存関係

- GUIから依存パッケージを追加・削除できる。
- 通常依存と開発用依存を区別できる。
- 実行前にコマンドと変更対象ファイルを確認できる。
- 成功後に`pyproject.toml`と`uv.lock`の状態が再表示される。

### AC-004：同期とツリー

- `uv sync`相当の処理を実行できる。
- 処理中のログと完了状態を確認できる。
- `uv tree`相当の依存関係を表示できる。

### AC-005：コマンド実行

- プロジェクトディレクトリを作業ディレクトリとして`uv run`相当のコマンドを実行できる。
- 標準出力、標準エラー、終了コード、実行時間を確認できる。
- 実行中のプロセスを停止できる。

### AC-006：uv未導入とエラー

- uv未導入時に操作を実行せず、OS別の公式導入案内を表示する。
- ネットワーク失敗、依存解決失敗、権限不足、Python不一致を区別して表示する。
- エラー時に再試行または設定確認へ進める。

### AC-007：クロスプラットフォーム

- macOS、Windows、Linuxでuvの検出、プロジェクト選択、パス処理、子プロセス実行の設計差異が文書化されている。
- OS固有のパスやシェル構文を前提にしない。

## 15. テスト要件

### 15.1 単体テスト

- uvパス検出とバージョン解析
- OS別パス正規化
- IPC入力のバリデーション
- コマンド引数の組み立て
- 終了コードとエラー分類
- ログのマスキング
- プロジェクト状態の判定

### 15.2 結合テスト

- `uv init`相当の新規プロジェクト作成
- `uv add`、`uv remove`による依存関係変更
- `uv sync`による仮想環境同期
- `uv tree`結果の表示
- `uv run`の標準出力・標準エラー取得
- 子プロセス停止と異常終了

### 15.3 UIテスト

- 新規プロジェクト作成導線
- 既存プロジェクト読み込み導線
- 実行前確認ダイアログ
- 成功、失敗、処理中、空状態、未導入状態
- キーボード操作とフォーカス表示
- 長いパス、長いパッケージ名、大量ログ
- ダークモードと狭いウィンドウ幅

### 15.4 OS別確認

- macOS：実行ファイル探索、macOSパス、ファイルダイアログ、プロセス停止
- Windows：`.exe`探索、Windowsパス、環境変数、PowerShell依存の回避
- Linux：実行権限、PATH、ディストリビューション差、デスクトップファイル連携の将来余地

## 16. 将来拡張

- uvのアプリ内導入または管理
- `uv python`によるPythonバージョン管理
- `uv tool`管理
- workspace管理
- `uv build`、`uv publish`
- 署名付き配布と自動更新
- 英語UI
- プロジェクト履歴と最近使用したプロジェクト
- プロジェクトテンプレート
- 複数パッケージの一括操作

## 17. 公式資料

- [Electron公式ドキュメント](https://www.electronjs.org/docs/latest)
- [uv公式ドキュメント](https://docs.astral.sh/uv/)
- [uv CLIリファレンス](https://docs.astral.sh/uv/reference/cli/)
- [uvプロジェクト作成](https://docs.astral.sh/uv/concepts/projects/init/)
- [uvプロジェクトの構造とファイル](https://docs.astral.sh/uv/concepts/projects/layout/)
- [uvのロックと同期](https://docs.astral.sh/uv/concepts/projects/sync/)
- [uv依存関係管理](https://docs.astral.sh/uv/concepts/projects/dependencies/)

